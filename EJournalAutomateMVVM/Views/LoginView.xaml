<Page x:Class="EJournalAutomateMVVM.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EJournalAutomateMVVM.Views" xmlns:vm="clr-namespace:EJournalAutomateMVVM.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginView">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Login:" VerticalAlignment="Center" Width="80"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Password:" VerticalAlignment="Center" Width="80"/>
            <PasswordBox x:Name="PasswordBox" Width="200"/>
        </StackPanel>
        <Button Content="Login" Grid.Row="2" Width="100" Command="{Binding AuthenticateCommand}" Click="LoginButton_Click"/>
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"/>
    </Grid>
</Page>
