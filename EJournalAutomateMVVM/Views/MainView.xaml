<Page x:Class="EJournalAutomateMVVM.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:EJournalAutomateMVVM.Views" xmlns:vm="clr-namespace:EJournalAutomateMVVM.ViewModels" d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <ComboBox x:Name="StatusComboBox" Grid.Row="0" SelectedIndex="{Binding SelectedStatusIndex, Mode=TwoWay}" VerticalContentAlignment="Center" FontSize="16">
            <ComboBoxItem>Все</ComboBoxItem>
            <ComboBoxItem>Непрочитанные</ComboBoxItem>
            <ComboBoxItem>Прочитанные</ComboBoxItem>
        </ComboBox>
        <TextBox x:Name="SearchTextBox" Text="{Binding SearchText, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" VerticalContentAlignment="Center" FontSize="14">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Resources>
                        <VisualBrush x:Key="PlaceholderBrush" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <Grid Background="Transparent">
                                    <TextBlock Text="Поиск" FontSize="14" Margin="2,0,0,0"/>
                                </Grid>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Style.Resources>
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Background" Value="{StaticResource PlaceholderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Background" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        <TextBox x:Name="CountTextBox" Text="{Binding MessageLimit, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" FontSize="14" VerticalContentAlignment="Center" Grid.Row="0" Grid.Column="2">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding ApplyLimitCommand}"/>
            </TextBox.InputBindings>
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Resources>
                        <VisualBrush x:Key="PlaceholderBrush" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <Grid Background="Transparent">
                                    <TextBlock Text="Количество писем" FontSize="14" Margin="2,0,0,0"/>
                                </Grid>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Style.Resources>
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Background" Value="{StaticResource PlaceholderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Background" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        <ListBox x:Name="EmailListBox" ItemsSource="{Binding FilteredMessages}" Grid.Row="1" Grid.ColumnSpan="3">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                        <CheckBox x:Name="EmailCheckBox" IsChecked="{Binding Selected}" VerticalAlignment="Center" Margin="0,0,0,0"/>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock x:Name="SenderTextBlock" Text="{Binding UserFrom.NameWithInitials}" FontSize="10"/>
                            <TextBlock x:Name="SubjectTextBlock" Text="{Binding Subject}" FontSize="16">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Unread}" Value="true">
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="LightGray"/>
                </Style>
            </ListBox.Resources>
        </ListBox>
        <Button x:Name="SelectAllButton" Grid.Column="0" Grid.Row="2" Content="Выбрать всё" FontSize="14" Command="{Binding SelectAllMessagesCommand}"/>
        <Button x:Name="DownloadButton" Grid.Column="2" Grid.Row="2" Content="Скачать" FontSize="14" Command="{Binding DownloadMessagesCommand}"/>

        <DockPanel x:Name="LoadingSplashPanel" Grid.RowSpan="3" Grid.ColumnSpan="3" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <DockPanel.Background>
                <SolidColorBrush Color="Gray" Opacity="0.9"/>
            </DockPanel.Background>
            <TextBlock x:Name="LoadingTextBlock" Text="{Binding LoadingMessage}" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="WrapWithOverflow"/>
        </DockPanel>
    </Grid>
</Page>
