<Window x:Class="EJournalAutomate.Views.Windows.DirectorySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EJournalAutomate.Views.Windows"
        mc:Ignorable="d"
        Title="EJournal Automate" Height="100" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <StackPanel VerticalAlignment="Center">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Путь к письмам:" Grid.Column="0" FontSize="14" TextAlignment="Center"/>
            <TextBox x:Name="CurrentDirTextBox" Grid.Column="1" VerticalContentAlignment="Center" Text="Путь" FontSize="14" IsEnabled="False"/>
        </Grid>
        <Button x:Name="ChangeDirButton" Content="Изменить путь" FontSize="14" Width="120" Margin="0,10,0,0" Click="ChangeDirButton_Click"/>
    </StackPanel>
</Window>
